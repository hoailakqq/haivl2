
<%= form_for @article, html: {multipart: true } do |f| %>
  <% if @article.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@article.errors.count, "error") %> prohibited this article from being saved:</h2>
      <ul>
      <% @article.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>
  <div class="form-group has-warning">
  <label class="control-label" for="inputWarning1">Tiêu đề</label>
  <span id="helpBlock2" class="help-block"><%= f.text_field :title %></span>
  
  </div>
  <div class="field">
      <%= f.label :image %><br>
      <%= f.file_field :image%>
    </div> 
  <div class="form-group has-warning">
  <label class="control-label" for="inputWarning1">Nội dung</label>
  <span id="helpBlock2" class="help-block"=><%= f.text_area :content ,size: "60x12"%></span>

  
  </div>
  
  <div class="actions">
    <%= f.submit 'Đăng bài', data: {cofirm: "are you sure ?"}%>
  </div>
<% end %>